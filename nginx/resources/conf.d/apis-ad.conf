upstream apis-ad.leon-grosse.fr {
    server apis-ad:8083;
}

server {
    listen 80;
    listen [::]:80;
    
    listen 443 ssl;
    ssl_certificate /etc/nginx/ssl/xxx.leon-grosse.fr.cer;
    ssl_certificate_key /etc/nginx/ssl/xxx.leon-grosse.fr.key;

    server_name apis-ad.leon-grosse.fr;

    access_log /etc/nginx/log/apis-ad-access.log;
    error_log /etc/nginx/log/apis-ad-error.log;

    location / {
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_pass http://apis-ad.leon-grosse.fr/;
    }
}