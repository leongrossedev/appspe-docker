upstream apis-google.leon-grosse.fr {
    server apis-google:8084;
}

server {
    listen 80;
    listen [::]:80;
    
    listen 443 ssl;
    ssl_certificate /etc/nginx/ssl/xxx.leon-grosse.fr.cer;
    ssl_certificate_key /etc/nginx/ssl/xxx.leon-grosse.fr.key;

    server_name apis-google.leon-grosse.fr;

    access_log /etc/nginx/log/apis-google-access.log;
    error_log /etc/nginx/log/apis-google-error.log;

    location / {
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_pass http://apis-google.leon-grosse.fr/;
    }
}